let altura = prompt("Ingresa el valor de la altura del rectángulo:");
let ancho = prompt("Ingresa el valor del ancho del rectángulo:");

let perimetro = 2 * (parseInt(altura) + parseInt(ancho));

alert("El perímetro del rectángulo es: " + perimetro);