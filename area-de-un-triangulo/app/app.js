let base = prompt("Ingresa el valor de la base del triángulo:");
let altura = prompt("Ingresa el valor de la altura del triángulo:");

let area = (base * altura) / 2;

alert("El área del triángulo es: " + area);